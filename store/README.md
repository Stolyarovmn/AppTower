# Store submission materials

Use the files in this directory to fill the Microsoft Edge Add-ons and Chrome
Web Store forms. They are source material for a reviewer, not runtime code.

`assets/screenshots/` is intentionally empty until screenshots are captured from
the signed-off running extension. Do not use mockups in store listings.
